# LiquiFact Frontend
# Must be an http(s) origin only. Do not include paths, query strings, or hashes.
NEXT_PUBLIC_API_URL=http://localhost:3001

# Stellar (when integrating wallet). Allowed values: testnet, public, futurenet.
# NEXT_PUBLIC_STELLAR_NETWORK=testnet
